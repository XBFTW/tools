Made this cmd file because ssh-copy-id isn't on windows. 

Just run this file, it assumes the user has a file named xbftwkey-public.pem in the ./ssh folder
it prompts for username, ip, and alias to auto put it in the user's ssh config
